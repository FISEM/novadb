# novadb — instructions for Claude

## Git

The user has authorized committing and pushing to `origin` automatically
in this repo — no need to ask for confirmation before `git commit` or
`git push origin master` here. Still follow the usual git safety rules
otherwise (no force-push, no `--no-verify`, no rewriting published
history, review `git status`/`git diff` before broad `git add`).
